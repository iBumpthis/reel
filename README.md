# reel
Local hosted media player built for playing mixes/concerts/mixtape videos, supports track marker imports and can run as a local or hosted web service on LAN. Supports network Read Only mounts for media, deployable to Linux and Windows. Also has (basic) visualizers because I missed them. Claude driven rebuild of TapeC after a full code review.
